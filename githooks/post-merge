#!/bin/sh
# Clear Python bytecode cache after merge to prevent stale .pyc issues.
# Install: git config core.hooksPath githooks
find ezexl3/ -name '__pycache__' -exec rm -rf {} + 2>/dev/null
echo "ezexl3: cleared __pycache__ after merge"
